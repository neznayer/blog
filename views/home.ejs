<%- include("partials/header"); -%>
<h1>Home</h1>
<% posts.forEach(item => { %>
<h3><%= item.title %></h3>
<p class="lead"><%= item.content.substring(0,100) + " ... " %></p> 
<a class="link-primary" href = "/posts/<%= item.title %>">Read more</a>
<% }); %>
<%- include("partials/footer"); -%>
